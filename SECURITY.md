# 安全策略

## 适用范围与免责

本仓库的代码是**教学参考实现**，不是产品。mini-codex 能执行 shell、写文件、驱动 MCP
工具——按书第 10–12 章的设计，这些能力都应受审批策略与沙箱约束；但教学代码永远不应
被当作边界完备的沙箱直接用于生产或处理敏感数据。

- **不要**在真实或敏感环境直接运行本仓库 `code/` 的未发布版本去执行不可信任务。
- **不要**在本仓库任何文件里提交 API key、token、证书或 `.env`。
- 合入默认偏保守的放行配置；任何“扩大权限”的改动都视同安全敏感改动评审。

## 报告漏洞

发现了可被利用的安全问题（不限于：越权执行、沙箱逃逸、路径穿越、提示注入导致命令
执行、secret 泄露路径），**请勿开公开 issue**：

1. 发邮件到维护者邮箱（见仓库主页个人资料 / 版权声明处）；
2. 或创建 issue 但**只写“安全相关问题”，不要贴细节**，等待维护者私联。

我们会：48 小时内确认收到 → 评估影响 → 修复并发布公告 → 之后你再决定是否公开细节。

## 通用提醒（写进 review 的问题）

对任何“会真实执行东西”的代码改动，review 时必问一句：
> “这段代码在一个被真实 LLM 诱导的环境里，可能被用来做什么？”

这正是本书正文反复训练读者问的问题。
