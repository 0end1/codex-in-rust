# 随书代码的编译基线（本地 / CI / 读者三方一致）。
# 锁定到具体 stable 版本（1.98.0，2026-08），保证可复现：
# 升级是显式动作 —— 改这里 → cargo test --workspace → 提交。
# 正文无更低 MSRV 承诺（语法下限叙事见 ch03 的 let-else 注释，Rust 1.65+）。
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
